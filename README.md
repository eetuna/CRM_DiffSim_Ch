# CRM_Dynamics

### This is the implementation of the multi-segment backwards solver of the CRM dynamics, using the older version of the CRMCPP model


### Greybox modeling method works only when initial is good enough, it works better without upper flexible segment moment.The parameter results are good enough for both cases generally.
